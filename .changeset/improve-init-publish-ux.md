---
"@agentrules/cli": patch
---

Improve init and publish command UX

- `agentrules init my-preset` now creates directory and initializes in one step
- Publish now shows the registry URL where your preset is live
